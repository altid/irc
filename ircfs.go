package ircfs

// This connects to IRC, manages interactions with the plugins 
func Register(name string, server.Server) {
    // Call fs library register function
    // do our initialization, setup if mdns/regsitry
    // Connect to IRC, start session
}

// network.Connect
// Set up Defaults for session here
// marshall our config with flagged sent in from cmd
// set up server
// create fs ctrl
// create buffer, listener, file listener, connect, etc
